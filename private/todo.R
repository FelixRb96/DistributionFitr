# TODO: globalfit: allow user to specify which packages to scan through; option: additional OR instead of
  # (1) specifies some packages already in FmailyList (default) --> fine
  # (2) specifies at least one that is unknown to FamilyList --> Option 1: getParams, append to FamilyList
  # 								 Option 2: ignore
